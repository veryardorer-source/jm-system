# JM OS — 제품 요구사항 문서 (PRD)

> 최종 업데이트: 2026-06-22 · 작성 기준: **실제 운영 DB**(Supabase `jm-system`) 확인 내용
> ⚠️ 기존 `ERD.md`/`0001`/`0002.sql`은 운영에 적용되지 않은 옛 설계임. 본 문서가 실제 기준.

## 0. 진행 현황 (2026-06-22)

**✅ 완료·배포됨** — 라이브: https://jm-interior.vercel.app
- **로그인 문제 해결**: 이메일 인증(Confirm email) OFF + 기존 미인증 계정 인증 처리
- **현장 상세 '현황' 대시보드 탭**(기본 탭): 요약카드(공정 진행률·자료/사진 수·누적 원가) + 공정 진행 막대 + 사진/영상 미리보기 + 배정직원·비용 카드. `field` 역할은 금액 자동 숨김. (기존 자료/공정/비용 탭은 유지)
- **영상 재생**: 시공사진 등에 올린 mp4가 썸네일(첫 장면)+▶로 표시, 클릭 시 재생 (자료 탭·현황 미리보기·라이트박스 모두 적용)
- **현장 수정 기능**: 현장 상세 "현장 수정" 버튼 → 이름/고객/담당/주소/단계/일정/메모 수정 (이전엔 진행단계만 변경 가능 → **주소 등 추가 입력 가능**)
- **가로 화면 허용**: PWA manifest `orientation` portrait→any (포터블 스크린은 앱 **재설치** 시 적용)

**배포 방법**: 이 PC에서 `cd jm-system && npx vercel --prod --yes` (Vercel 로그인·`jm-interior` 링크 완료). GitHub 자동배포는 계정 분리(veryardorer vs veryardorer-source) 이슈로 보류.

**⏳ 요청됨·미착수**
- 직원 채팅(전체 단톡방 우선) · 자료 업로드 알림(앱 내 🔔)
- 영수증/출금을 **현장별로 연결** (현재 DB에 `project_id` 없음 → DB 변경 필요)
- 앱 내 가입 승인(4장) · 모바일 반응형 · "신규 가입 허용" OFF

## 1. 개요

JM 건축인테리어의 **사내 관리 웹앱**. 노션 기반 관리 시스템을 대체하며, 외주 없이 Claude와 함께 직접 구축·운영한다. 비개발자인 대표가 화면을 보며 피드백해 다듬는 방식.

- **목적**: 현장(프로젝트) 중심으로 일정·자료·비용·직원 업무를 한곳에서 관리.
- **사용자**: JM 건축인테리어 임직원 (대표/관리자, 디자인, 현장).
- **배포**: Vercel (https://jm-interior.vercel.app). 기술스택: Next.js + Tailwind + Supabase(DB/Auth/Storage).

## 2. 사용자 & 역할 (권한)

권한은 `profiles.role` 값으로 결정된다. 현재 실제 사용 값:

| role | 의미 | 권한 개요(의도) |
|------|------|----------------|
| `admin` | 대표·관리자 | 전체 조회/수정, 재무, 직원·권한 관리, 출금 승인 |
| `designer` | 디자인팀 | 현장·자료·일정 관리, 영수증/출금 요청 |
| `field` | 현장팀 | 배정된 현장 업무·자료 업로드·영수증/출금 요청 (재무·민감정보 제한) |

- `profiles.team` 컬럼으로 팀 구분 가능(현재 데이터는 비어 있음).
- 신규 가입자는 기본 `field`로 생성됨.

## 3. 핵심 기능 (실제 데이터 모델 기준)

| 영역 | 기능 | 관련 테이블 |
|------|------|-------------|
| 계정/권한 | 로그인, 직원 프로필·역할·팀 | `profiles` |
| 인사 | 직원 정보 관리 | `employees` |
| 현장(프로젝트) | 현장 등록·상태·정보 | `projects` |
| 현장 배정 | 현장별 담당 직원 배정 | `project_assignments` |
| 공정/일정 | 일정·공정표 | `schedules` |
| 현장 자료 | 사진/도면/문서 업로드 | `project_files` |
| 현장 원가 | 현장별 비용 기록 | `project_costs` |
| 영수증 | 영수증 사진·금액 업로드 | `receipts` |
| 출금 요청 | 거래처 지급/출금 요청·승인 | `withdrawal_requests` |
| 공지 | 사내 공지사항 | `notices` |
| 회사 문서 | 매뉴얼·회사 문서 | `company_documents` |
| 재무 | 매출/고정비/급여/현장수익 | `finance_sales`, `finance_fixed_costs`, `finance_payroll`, `finance_project_profit` |

## 4. 가입 · 인증 · 권한 정책

### 현재 동작 (2026-06-22)
- 이메일+비밀번호 회원가입. **이메일 인증(Confirm email) OFF** → 가입 후 바로 로그인.
- 가입 시 `profiles` 행 자동 생성(기본 role=`field`).
- **승인(pending) 절차 없음** → 가입=즉시 사용. 역할 변경은 현재 대표가 DB에서 직접.
- "신규 가입 허용(Allow new users to sign up)" 현재 ON(외부인도 가입 가능 → 직원 가입 끝나면 OFF 권장).

### 요청된 개선 — 앱 내 가입 승인 (예정)
대표가 **앱 안에서** 신규 직원을 승인하고 역할을 부여하는 흐름.
1. 가입 → `profiles.status='pending'` (상태 컬럼 신규 추가 필요)
2. pending 직원은 로그인해도 "대표 승인 대기" 화면만 표시
3. 관리자 전용 "직원 승인" 화면에서 승인 + 역할/팀 지정 → `status='approved'`
- ⚠️ DB 변경(①)과 앱 화면(②)은 **반드시 함께 배포**. (상세: `actual_schema_and_approval_plan.md`)

## 5. 업무 규칙
- 회계/급여는 외부 경리나라에서 대표가 처리하던 방식이나, 실제 앱에는 `finance_*` 재무 기능이 존재함 → 실제 운영 범위는 다음 세션에 대표와 재확인 필요.
- 직원은 영수증 사진만 올리고, 대표가 확인.

## 6. 비범위 (현재 의도적으로 제외/보류)
- 고객관리(CRM), Make 자동알림.
- ~~AI 자동 블로그~~ → **범위에 포함됨** (9장 SNS/블로그 마케팅 참고).

## 7. 데이터 모델 (운영 public 스키마, 15개 테이블)
```
company_documents, employees, finance_fixed_costs, finance_payroll,
finance_project_profit, finance_sales, notices, profiles,
project_assignments, project_costs, project_files, projects,
receipts, schedules, withdrawal_requests
```

## 8. 로드맵 / 다음 작업
- [x] 앱 소스 확보 (`veryardorer-source/jm-system` clone)
- [x] 현장 상세 '현황' 대시보드
- [x] 영상 재생·미리보기
- [x] 현장 수정(주소 등)
- [x] 가로 화면 허용 (PWA manifest)
- [x] 라이브 배포 파이프라인 (Vercel CLI)
- [x] **직원 채팅** (전체 + 그룹방 + 1:1, 실시간)
- [x] **알림** (자료 업로드·공지·채팅 → 🔔, 실시간)
- [x] 사진 구역(층/공간)별 묶기 · 분류 직접 입력
- [x] PWA 공유 대상(카톡 등 → JM관리로 사진 바로 저장)
- [x] 보안(RLS) 전체 적용
- [ ] **SNS/블로그 포스팅 관리** (9장) — 현장별 디자인/시공/마감 상태판 → AI 자동 초안
- [ ] **영수증/출금 현장별 연결** (DB에 project_id 추가)
- [ ] 모바일 반응형 (☰ 햄버거 메뉴, 공정표 카드형) — `mobile_audit.md`
- [x] **가입 승인 게이트** — 권한 부여 전(role=pending/미지정)에는 '승인 대기' 화면만, 전 페이지 접근 차단 (AuthGate). 관리자가 직원관리에서 역할 부여 시 사용 가능. (UI 레벨)
- [ ] 직원 가입 완료 후 "신규 가입 허용" OFF
- [x] RLS 역할별 세분화 (현장팀: 주민번호/계좌·재무 차단) — 2026-07-02, `db/rls_sensitive.sql`+`db/rls_money.sql`+`db/rls_chat.sql`. 상세: `docs/security_status.md`
- [ ] 재무(`finance_*`) 기능 실제 사용 범위 대표와 확정
- [ ] **재정관리 "한눈에 보기"(통장 잔액·이번 달 고정지출·급여) 강화** (10장) — 추후 구체화 후 개발
- [x] **권한(역할) 체계** — admin/designer/field/partner, 외부협력업체=현장 관련(금전 제외) 보기 전용 (11장)
- [x] **웹 푸시 알림** — 앱 꺼져도 PC·모바일 OS 알림 (12장)
- [x] **채팅 파일 전송·드래그앤드롭·대화별 안읽음 배지** (12장)
- [x] **작업일지**(오늘/내일 업무·특이사항·메모) (12장)
- [x] **직원별 급여·근태**(직원정보내역) (12장)
- [x] **재정관리 견적서 탭**(옛 견적서 파일 보관) (12장)
- [x] **HEIC 사진 자동 변환 표시** · **업로더 자동 표시** (12장)
- [x] **모바일 하단메뉴 정리**(핵심4+더보기)
- [ ] `middleware.ts` → `proxy` 로 이름 변경 (Next.js 16 권장, 경고만·동작 정상)
- [~] **급여대장 엑셀 업로드**(재정관리 급여내역) — 진행 중. 인원·급여합계 인식 OK, **실지급(차감지급액) 컬럼 매핑 오류 수정 필요**(12장 참고)

## 9. SNS / 블로그 마케팅 (신규 — 2026-06-22 추가)

### 배경
회사가 블로그·인스타 등 SNS 마케팅을 운영. **네이버 블로그는 현장 1곳당 3개 포스팅**(디자인 / 시공 / 마감), 주 3회 발행, 네이버 SEO 최적화 전략. ([[project-blog-strategy]] 메모 참고)

### 요구사항
1. **현장별 포스팅 관리(상태판)**: 현장마다 채널(블로그·인스타) × 유형(**디자인 / 시공 / 마감**)별로 **상태(계획중 → 작성중 → 발행완료)** 추적. 발행 링크·발행일 기록.
2. **사진 기반 자동 포스팅(자동화)**: 현장 자료(시공전/시공/마감 사진)를 골라 → 그 사진들로 **디자인/시공/마감 블로그 초안(제목+본문)을 AI가 자동 생성**.

### 단계 계획
- **1단계 — 포스팅 상태판** (개발 가능)
  - DB: `sns_posts`(project_id, channel, post_type[디자인/시공/마감], status[계획중/작성중/발행완료], title, content, link, published_at)
  - UI: 현장 상세에 'SNS' 탭 또는 별도 `/marketing` 페이지. 현장×유형별 상태 한눈에, 링크 저장.
- **2단계 — 사진 → AI 초안 생성** (Claude API 연동 필요)
  - 사진 선택 → AI가 블로그 제목·본문 초안 작성 → 검토 후 복사/발행.
  - 기존 `JM_blog` 앱(네이버 블로그 AI 작성기)과 **중복/통합 검토** 필요 (자산 재활용 가능).

### 결정 필요 (대표 확인)
- 블로그 발행을 **앱에서 직접 발행**할지, **초안만 만들고 네이버에 복붙**할지 (네이버 공식 API 제약 있음 → 복붙 방식이 현실적)
- 인스타도 1단계 상태판에 포함할지 (발행은 수동, 상태만 관리)
- 기존 JM_blog 앱과 통합 vs JM OS 내 신규 구현

## 10. 재정관리 강화 — "이사님이 즉시 보는 화면" (신규 — 2026-06-30 추가, 추후 구체화)

### 배경
재정관리(`/admin/finance`, 관리자 전용)에는 현재 **고정지출 / 급여내역 / 현장별 이익 / 매출매입** 탭이 있음. 여기에 **이사님이 열자마자 바로 파악**할 수 있는 요약 화면을 추가하고 싶음.

### 요구사항 (대표 정리)
1. **통장 잔액(현재 잔액)** 확인 — 통장에 얼마 남아있는지.
2. **매월 고정지출** 이 얼마나 나가는지.
3. **매월 나간 급여** 가 얼마인지.
4. 위 내용을 **한 화면에서 즉시** 볼 수 있게 (요약 대시보드).

### 구현 아이디어 (초안 — 확정 아님)
- 재정관리 맨 앞에 **"한눈에 보기"** 탭 추가: 통장 잔액 합계 + 이번 달 고정지출 합계 + 이번 달 급여 합계 + (잔액에서 차감한) 가용 현금.
- 통장 잔액은 은행 연동이 없으므로 **관리자가 직접 입력/갱신**(계좌별 잔액 + 기준일). 새 테이블 `finance_balances`(account_name, balance, as_of, memo) 필요.

### 검토/결정 필요 (추후 정리)
- **급여를 어디서 관리?** — 제안: 급여의 *금액/지급 흐름*(매월 총액)은 **재정관리(급여내역 탭, 현행 유지)**. 직원 *개인정보·계좌·근태*는 **직원정보내역**. → 돈은 재정관리, 사람은 직원정보로 분리.
- **근태(조퇴/결근/연차) 관리** — 직원정보내역 쪽에 직원별 근태 기록(새 테이블)으로 두는 안. 별도 기능이라 범위·항목 추후 확정.
- 통장 잔액을 **수동 입력**으로 충분한지(엑셀/은행 캡처 첨부 병행?), 계좌를 몇 개까지 관리할지.
- ~~재정관리 데이터의 DB 레벨 접근제한~~ → **완료(2026-07-02)**: employees·급여·근태·finance_* 는 `db/rls_sensitive.sql`로 **admin 전용 RLS** 적용됨(더 이상 authenticated 공통 아님).

> 상태: 대표가 항목을 조금 더 정리한 뒤 개발 예정. 지금은 문서화만.

## 11. 권한(역할) 체계 (2026-07-10 전면 갱신)

### 역할 종류
가입(회원가입)하면 `pending`(승인 대기)으로 생성 — **아무 업무 데이터도 못 봄**(AuthGate '승인 대기' 화면 + DB RLS 차단). 관리자가 **회원 관리(`/admin/users`)**에서 역할을 부여해야 사용 가능. (DB `profiles.role`)

| 역할 | 대상 | 요약 |
|------|------|------|
| **admin (관리자)** | 대표/이사 | 전 기능 + 관리자 메뉴(회원 관리·직원정보내역·경영관리) + **수금 관리** |
| **designer (디자인팀)** | 내부 직원 | 현장·비용 포함 대부분. 수금·급여·재정·직원정보는 차단 |
| **field (현장팀)** | 내부 직원 | 현장 실무 전반. 금액류(수금·비용)·급여·재정 차단 |
| **partner (외부협력업체)** | 외부 회사 | **공개 지정된 현장만, 보기 전용** |
| **pending (승인 대기)** | 신규 가입 | 전면 차단 |

### 메뉴/기능별 매트릭스 (✅=가능 · 👁=보기만 · ❌=차단)
| 메뉴 | admin | designer | field | partner |
|---|:---:|:---:|:---:|:---:|
| 대시보드 | ✅ | ✅ | ✅ | 👁 (공개 현장만, 금액 없음) |
| 공지사항 | ✅(등록·수정·삭제) | ✅ | ✅ | 👁 '사용법'만 |
| 현장 관리 | ✅ | ✅ | ✅ | 👁 **공개 지정 현장만** |
| └ 비용 탭 | ✅ | ✅ | ❌ | ❌ |
| └ 🔒 외부공개 설정 | ✅ | ✅ | ✅ | ❌ |
| 작업일지 | ✅ | ✅ | ✅ | ❌ |
| 영수증 | ✅ | ✅ | ✅ | ❌ |
| 출금 요청 | ✅ | ✅ | ✅ | ❌ |
| **수금 관리** | ✅ | **❌** | **❌** | ❌ |
| 회사 서류 | ✅ 전체+쓰기 | 👁 전체공개만 | 👁 전체공개만 | ❌ |
| 거래처 | ✅ | ✅ | ✅ | ❌ |
| 채팅 | ✅ | ✅ | ✅ | ❌ |
| 통합검색 | ✅ | ✅(수금 제외) | ✅(금전 제외) | ❌ |
| 회원 관리·직원정보내역·경영관리 | ✅ | ❌ | ❌ | ❌ |

- **수금 관리 = admin 전용** (2026-07-10 대표 확정): 메뉴 숨김 + 페이지 차단 + 검색 제외 + **알림도 admin에게만** + RLS.
- 알림: 금액이 body에 없는 알림은 전원 발송, 비용 등록 알림은 금액 미노출, 수금 알림은 admin만.

### 외부협력업체(partner) — "공개 지정된 현장만, 보기 전용" (잔디식)
- **현장별 공개**: 현장 등록 시 공개할 업체 체크, 이후 현장 상세 → **🔒 외부공개**에서 초대/내보내기(직원 누구나 관리). 미지정 현장은 **목록에도 안 나오고 직접 URL도 차단**(`project_access` + RLS).
- 볼 수 있음: 공개 현장의 현황·자료·공정(비용 제외), 공지 '사용법', 알림.
- 편집 전면 불가(`canEdit()`), 채팅·금전·서류·거래처·검색 차단.

### 구현 방식 — 화면 + DB 이중 잠금
- **화면**: `canEdit()`/`isApproved()`(`lib/auth-context.tsx`), 메뉴 필터 `PARTNER_HIDDEN`/`ADMIN_ONLY_NAV`(`components/Sidebar.tsx`), 페이지별 role 가드.
- **DB(RLS)**: 화면과 동일 기준을 DB가 강제 — URL 직접 요청·기술적 우회도 차단.
  - admin 전용: 급여·근태·직원정보·finance_*·**payments** (`rls_sensitive.sql`, `rls_money.sql`)
  - 역할별: receipts/withdrawals(admin·designer·field), project_costs(admin·designer), company_documents, project_files, contacts, work_logs (`rls_money.sql` 등)
  - 참여자 기준: 채팅 전체 (`rls_chat.sql`) / 본인만: notifications·chat_reads·push_subscriptions
  - partner 현장별: projects/schedules/assignments/project_files (`project_access.sql`)
  - 역할·테이블별 전체 표와 적용 상태: **`docs/security_status.md`** (단일 기준 문서)

### 추후 검토
- Storage(uploads) 비공개 버킷 + signed URL (현재 public — security_status.md 참고).
- 감사 로그(삭제·금액수정 이력).

## 12. 구현 완료 기능 (2026-06-29~30 대량 추가)

지난 이틀간 대표 피드백으로 추가·수정된 기능 정리. (모두 배포 완료, 앱 저장소 `jm-system` master)

### 알림
- **웹 푸시(Web Push)** — 앱이 꺼져 있어도 PC·모바일에 **OS 시스템 알림** 팝업. 서비스워커 push/notificationclick, `push_subscriptions` 테이블, `/api/push/send`(web-push+VAPID). 각 직원이 알림 페이지의 **"알림 켜기"**를 한 번 눌러야 구독됨. *아이폰은 홈 화면 설치(PWA) 후에만 백그라운드 푸시 가능(애플 제약).*
- **앱 내 토스트 + OS 알림** — 앱 열려있을 때 우상단 팝업.
- **알림 대상 확대** — 현장자료·공지·채팅뿐 아니라 **영수증·출금·수금·작업일지·회사서류** 업로드 시에도 전원 알림.

### 채팅
- **파일 전송**(📎, 문서/PDF/엑셀 등) + **드래그앤드롭**(PC).
- **대화별 안읽음 배지** — 좌측 목록(전체/방/1:1)에 새 메시지 표시, 열면 사라짐. 사이드바 채팅 메뉴에도 안읽음 숫자.

### 출금요청 / 공유
- 카톡 **글(텍스트) 공유** 수신 → 사유칸 자동입력, 사진 없이 글만 저장 가능.
- 출금 건 **글·사진 개별 수정/추가/삭제**, 모바일 삭제 수정.
- 사진+글 한 건으로 등록(앱 내 추가가 아이폰·안드로이드 공통 방법).

### 현장 / 사진
- **HEIC(아이폰 원본) 사진 자동 변환 표시**(HeicImg) — 안 보이던 옛 HEIC 사진 정상 표시.
- **업로더 자동 표시** — 자료/사진/영수증/출금에 올린 사람 이름 자동 기록·표시.

### 작업일지 (신규)
- `/worklogs`, 사이드바 📒. 항목: 날짜 · 오늘 한 업무 · 내일 해야할 업무 · **특이사항** · 메모. 작성자 자동. `work_logs` 테이블.

### 재정관리
- **견적서 탭**(신규) — 현장명 · 금액 · **첨부파일**(옛 견적서 PDF/엑셀/사진 업로드). 엑셀은 MS 오피스 온라인 뷰어로 바로 열기. `finance_quotes` 테이블.
- **급여대장 엑셀 업로드 (⚠ 진행 중 — 2026-07-01 시작, 내일 마무리 예정)**
  - 목적: 회사가 쓰는 **급여대장 엑셀(다중 시트 파일)을 통째로 업로드** → 그중 "급여대장" 시트만 자동으로 읽어 저장. 나중에 **월별 총 급여**와 **개인별 급여 변동(월별 표, 전월대비 ▲▼)** 조회.
  - 구현: `lib/excel-parse.ts` `parsePayrollLedger()` — "급여대장" 시트에서 월(예: "2026년 7월", 날짜형식 셀도 raw:false로 문자 인식) + 성명 + 기본급 + 급여합계 + 차감지급액 추출. 인식 실패 시 **월 직접 선택** 가능. 저장은 기존 `finance_payroll`(월·직원·금액)에 급여합계=amount, 실지급/기본급=memo로 넣고 **같은 달은 교체**.
  - 재정관리 급여내역 탭: **📋 급여대장 업로드** 버튼, **월별/개인별 전환**, 개인별 피벗표(`PayrollPivot`).
  - ~~알려진 문제: 실지급 값 상이~~ → **해결(2026-07-07, 16장)**: 급여대장 **전체 시트 보관**(finance_payroll_ledger)으로 원본 그대로 표시. 월 자동인식(오늘날짜 수식 셀 오인) 문제도 **월 직접 선택**으로 전환, 같은 달 재업로드 중복 방지.

### 직원정보내역
- **직원별 급여·근태** — 직원 이름 클릭 → 상세(급여 내역 + 근태[지각/조퇴/결근/연차/반차/기타]). `employee_salaries`, `employee_attendance` 테이블. (급여=돈 흐름은 재정관리, 개인 급여·근태는 직원정보내역으로 분리.)

### 권한 강화
- **외부협력업체 범위 축소** — 현장 관련(금전 제외)만, 채팅·작업일지·서류·영수증·출금·수금 숨김+주소 차단, 공지 '사용법'만.
- **가입 승인 게이트** — 권한 부여 전 '승인 대기' 화면만.

### 기타
- **모바일 하단메뉴 정리** — 핵심 4개(홈·현장·채팅·알림) + ☰ 더보기(나머지+관리자메뉴).
- PC 설치법 공지 등록, 데스크탑 PWA 설치 안내.

### 신규 테이블 (public)
`payments`(수금), `work_logs`, `employee_salaries`, `employee_attendance`, `push_subscriptions`, `finance_quotes`, (+messages에 file_url/file_name, work_logs에 special_notes 등 컬럼 추가).

### 알려진 제약 / 다음
- 웹 푸시: 아이폰은 홈화면 설치 필수. 각 기기 "알림 켜기" 1회 필요.
- 채팅 안읽음은 기기별 localStorage 기준(다기기 약간 차이 가능).
- `middleware.ts` → Next.js 16이 `proxy`로 이름 변경 권장(경고, 동작엔 문제 없음).
- 재정관리 "한눈에 보기"(통장잔액 요약)는 아직 미구현(10장).

## 13. 개선 로드맵 — 외부 리뷰(2026-07-02) 반영, 보안 우선

민감정보(주민번호·계좌·급여·재정)가 실제로 들어가는 단계라 **기능 추가보다 권한/보안 정비가 1순위**.

### A. 보안 (최우선)
- [x] **민감 테이블 DB(RLS) 관리자 전용 — 적용 완료(2026-07-02)** — `db/rls_sensitive.sql` 실행됨(`my_role()` 헬퍼 + employees/employee_salaries/employee_attendance/finance_* admin 전용). 로그인만 한 사용자가 URL로 직접 요청해도 차단. 관리자 전용 화면 테이블이라 앱 영향 없음.
- [x] **알림(notifications) RLS — 적용 완료(2026-07-02)** — `db/rls_notifications.sql`. 본인 알림만 select/update/delete, insert는 로그인 사용자 허용(알림 발송 유지).
- [x] **채팅(messages) RLS — 적용 완료(2026-07-02)** — `db/rls_chat.sql`: messages/chat_rooms/chat_room_members/message_reactions/chat_reads 참여자·본인 기준. 헬퍼 `is_room_member`/`can_see_message`(security definer)로 재귀 방지. 대표 실행 확인·앱 정상.
- [x] **금전/내부자료 RLS — 적용 완료(2026-07-02)** — `db/rls_money.sql`: receipts/withdrawal_requests/payments=admin·designer·field, project_costs=admin·designer, company_documents=admin전체·그외 전체공개, project_files=승인자 읽기·비파트너 쓰기. 현재 화면 권한과 일치(기능 안 깨짐). 대표 실행 확인·앱 정상.
- [x] **푸시 발송 API 대상자 검증 — 완료(2026-07-02)** — `/api/push/send`가 클라이언트 `userIds`를 안 믿고 `{event: dm|room|mention|broadcast}`로 **서버가 수신자 계산·검증**(DM 당사자/방 멤버십/승인역할). 인앱 알림 행도 서버(service role)에서 생성. `lib/notify.ts` = notifyOthers/notifyDM/notifyRoom/notifyMention.
- [ ] **partner(외부협력업체) DB 레벨 제한** — 현재 UI 차단 + project_files 쓰기 차단(rls_money). "배정된 현장만"은 `project_assignments`에 `user_id` 없음 → 컬럼 추가 후 RLS 필요(보류). 상세: `docs/security_status.md`.
- [ ] **Storage 버킷 보안** — `uploads`가 public. 계약서·직원자료·경리자료는 **private bucket + signed URL**로. (사진 표시 로직 전면 영향 → 카테고리별 분리 검토.)
- [ ] **가입 정책 정리** — 로그인 화면 "회원가입" 노출. 관리자 초대 방식이면 숨기거나 승인대기 흐름 강조(AuthGate는 이미 있음).
- [ ] **감사 로그** — 현장 상태 변경·파일 삭제·금액 수정 이력(누가/언제).

### B. 실무 기능 (효과 큰 순)
- [ ] **출금요청 승인/반려 워크플로우** — 요청→관리자 승인/반려→처리완료→알림→이력. (`withdrawal_requests.status` 이미 있음: 요청/확인중/처리완료 활용.)
- [ ] **현장별 손익 요약** — 현장 상세에 계약금액/누적수금/누적비용/예상이익/미수금 카드. (payments·project_costs·finance_project_profit 연결.)
- [ ] **자동 알림** — 공정 종료 D-1, 완료예정일 지남, 잔금 미수, 출금 승인 대기 등(스케줄 기반).
- [x] **통합 검색**(`/search`) — 현장·자료·공지·작업일지·(금액권한 시)영수증/출금/수금 한 번에 검색. 외부협력업체 차단.
- [ ] **고객 관리** — 고객명·연락처·주소·상담내용·AS이력·선호스타일. (재방문/소개 대응, 새 `customers` 테이블)
- [ ] **AS 관리** — 완료 현장 AS: 접수→방문예정→처리완료, 사진 첨부·담당자 배정. (새 `as_requests` 테이블)
- [ ] **자동 리포트** — 매주 월요일 요약(진행/지연 현장, 이번 주 일정, 미수금, 출금 대기). (기존 데이터 집계, 새 테이블 불필요)
- [ ] **고객/거래처 연락처 관리** — 연락처·주소·AS메모·거래처 담당자·분야. (고객 관리와 통합)
- [ ] **견적서/계약서 버전 + 계약금/중도금/잔금 + 계약서 파일** — 수금관리와 연결(현재 견적서 탭은 파일 보관 수준).
- [ ] **사진 UX** — 업로드 시 리사이즈, 대표사진 지정, 카톡 공유용 선택 묶음.

### C. 완성도
- [ ] **아이콘 정비** — 이모지 → lucide 선형 아이콘(직원·거래처 공유 화면 신뢰감).

> 리뷰 총평: "만들다 만 앱"이 아니라 실제 업무 흐름을 잘 반영. 다음 단계는 기능 추가보다 **보안(RLS·storage)·출금 승인·현장 손익 요약·알림 자동화** 다듬기가 효과 큼.

## 14. 채팅 "잔디(JANDI)화" — 협업 메신저 수준 확장 (2026-07-02)

대표 요청: "협업툴 잔디에 있는 기능은 다 사용가능했으면 좋겠어." 화상통화·외부연동(캘린더/웹훅)은 소규모 사내툴에 과함·유지비 커서 제외, 나머지 실무 기능을 이식. (모두 배포 완료, 저장소 `jm-system` master)

### 업로드/보기 통일 (선행 작업)
- 공용 단일파일 업로드칸 `FileDropInput` — **클릭 · 드래그 · Ctrl+V 붙여넣기(PC 캡처)** 지원. 회사서류·견적서·매출매입에 적용.
- 사진 뷰어 통일 — 다운로드 없이 앱 안에서 **큰 화면 + 좌우 넘김 + 내보내기(공유)/저장**. 영수증·출금·수금·현장사진·채팅 사진 동일 패턴. (`lib/media.ts`: viewInBrowser/shareUrl/downloadUrl)

### 추가된 채팅 기능
- **답장(인용)** — 특정 메시지에 답글, 인용 블록 클릭 시 원문으로 스크롤 이동. (`reply_to_id`, `reply_preview`)
- **이모지 반응** — 👍❤️😂😮🙏✅ 토글, 개수 표시, 실시간. (`message_reactions` 테이블)
- **@멘션** — 입력창 옆 @ 버튼으로 직원 선택 삽입, 언급된 사람에게 알림+푸시, 텍스트 강조.
- **메시지 수정/삭제** — 내 메시지 수정(수정됨 표시)·삭제(소프트 삭제, "삭제된 메시지입니다"). 관리자는 전체 삭제 가능. (`edited_at`, `is_deleted`)
- **공지 고정** — 메시지 상단 고정, 노란 바 표시·클릭 시 이동. (`pinned`)
- **대화 검색** — 헤더 🔍, 방 안 내용 필터.
- **방 관리** — 방 이름 변경, 멤버 추가/내보내기, 나가기(⚙️, 채팅방 한정).
- **읽음 확인** — 내가 보낸 메시지에 1:1='읽음' / 단체방=안 읽은 사람 수(전원 읽으면 '읽음'), 실시간 감소. 전체 채팅방은 인원多로 생략. (`chat_reads` 테이블: user_id+conv_key별 마지막 읽은 시각)
- 채팅 화면에선 통합검색 FAB 숨김(전송 버튼 겹침 해소).

### 신규 테이블 / 컬럼
- `message_reactions`(message_id, user_id, emoji, unique) — `db/chat_features.sql`
- `messages` 컬럼 추가: reply_to_id, reply_preview, is_deleted, edited_at, pinned — `db/chat_features.sql`
- `chat_reads`(user_id, conv_key, last_read_at, PK(user_id,conv_key)) — `db/chat_reads.sql`
- 실시간 publication에 message_reactions·chat_reads 추가. **초기엔 permissive였으나 2026-07-02 `db/rls_chat.sql`로 참여자·본인 기준 RLS로 교체됨.**

### 남은 채팅 과제
- ~~messages RLS 미적용~~ → **완료(2026-07-02, `db/rls_chat.sql`)**: messages/chat_rooms/chat_room_members/message_reactions/chat_reads 참여자·본인 기준, insert는 승인 실무역할만(partner·pending 차단). 상세·검증쿼리: `docs/security_status.md`.
- 읽음 표시는 상대의 "대화 열람 시각" 기준(개별 메시지 단위 아님) — 실무 충분하나 카톡식 메시지별 정밀도는 아님.
- 안읽음 좌측 배지는 여전히 기기별 localStorage(읽음확인 DB와는 별개).

## 15. 견적 기능 범위 기준 (2026-07-02 확정)

**JM 관리시스템 본체에 포함되는 견적 기능 = "견적서 파일 첨부·보관"까지만.**
- 현장 상세 → 자료 탭에 **`견적서`·`계약서` 카테고리**(도면·3D·사진과 동일한 파일 보관용).
- 견적서/계약서 PDF·엑셀 파일 **첨부 / 보기 / 다운로드 / 공유**.
- (별개로 재정관리 §12에 `견적서 탭`(finance_quotes)이 있으나, 이 역시 "현장명·금액·첨부파일 보관" 수준 — 계산 로직 아님.)

**본체에 넣지 않는 것 = "견적 프로그램"(별도 프로젝트).**
- 견적서 직접 작성, 공종/품목/단가 입력, 자동 계산, 단가표(`price_book`) 관리, 엑셀 견적서 생성, 개략견적 계산기.
- 관련 코드: `/estimates` 라우트, `db/estimates.sql`, `src/lib/estimate.ts`, `src/lib/estimate-excel.ts`, `exceljs` 의존성.
- **위치**: `jm-system` 저장소의 **`estimate-program` 브랜치**에 보존(2026-07-02 분리). 향후 별도 저장소로 이관 가능. 본체(master)에는 병합 금지.

**규칙**: master에는 `/estimates` 라우트·견적 계산 로직·`price_book`·`exceljs`를 두지 않는다. 견적 "작성/계산"이 필요하면 estimate-program(별도)에서 진행.

## 16. 운영 편의 기능 대량 추가 (2026-07-02 ~ 07-07)

대표 피드백으로 추가·수정. (모두 배포 완료, `jm-system` master)

### 출금요청 — 처리 여부 확인
- 카드마다 **⏳ 대기 / ✅ 처리완료** 배지(대기=노란 테두리, 완료=흐림). 헤더에 **대기 N건** + 전체/대기/완료 필터 탭.
- **관리자만** `✓ 처리완료` / `↩ 되돌리기` 토글(카드·상세보기 둘 다). 기존 `status` 컬럼('요청'/'처리완료') 활용 — DB 변경 없음.

### 급여대장 — 전체 시트(수당·공제 항목) 보기
- **급여내역 3뷰**: 월별 / 개인별 / **급여대장**. 급여대장 뷰 = 업로드한 원본 표 그대로(성명~차감지급액 전 컬럼, 공제항목 빨강·차감지급액 초록 강조, 총합계 행 포함).
- 새 테이블 `finance_payroll_ledger`(month PK, headers/rows/total jsonb, **admin 전용 RLS**) — `db/payroll_ledger.sql`. 업로드 시 요약(finance_payroll)+전체 시트 동시 저장.
- 파서 `parsePayrollLedgerFull` — 성명~차감지급액 구간 전 컬럼, 뒤쪽 참고컬럼(연금상한 등) 제외. 실제 3월 파일(28개 항목)로 검증.
- **월은 항상 직접 선택**(자동인식 제거 — 시트의 월 칸이 '오늘 날짜' 수식이라 7월로 오인되던 문제). 같은 달 재업로드=교체(삭제 실패 시 중단, 중복 방지).
- **개인별 표 기본 = 실지급(공제 후)**, 실지급↔세전 토글. 실지급은 업로드 시 메모("실지급 X원")에서 추출, 없으면 급여합계 대체.

### 현장별 이익 — 손익표 파일 첨부
- 이익 자료에 **엑셀(xlsb 포함)·PDF 첨부** → 표에서 📎 클릭 시 다운로드 없이 열람(오피스/구글 뷰어). `finance_project_profit`에 file_url/file_name — `db/profit_file.sql`.
- 손익표 구조 확인: 견적서와 동일 4시트 + 식대외. 공종별집계표에서 **금액 열=견적, 단가 열=실지출**. 예시 분석(롯데캐슬 미용실, 진행중): 계약 20,980,000(VAT외) − 실지출 10,172,350 = 현재 이익 10,807,650(51.5%).

### 채팅
- **나와의 채팅** — 카톡 '나에게 보내기'식 메모·사진·파일 보관함(자기 자신 DM). 알림·읽음표시 없음. 목록 상단 고정.
- **링크 미리보기** — URL 보내면 제목·설명·이미지 카드(카톡식). `/api/link-preview`(로그인 필요, og 태그 파싱, 사설망 차단, 2MB까지 읽기 — 유튜브처럼 태그가 뒤에 있는 사이트 대응, CDN 캐시 1일). 예전 메시지도 자동 적용.

### 사진/업로드 UX
- **모바일 사진 선택 모드** — 자료 탭 `☑︎ 사진 선택` 토글: 켜면 사진 탭=선택(작은 체크 정확히 안 눌러도 됨). 체크 1회로도 자동 진입. 내보내기 폴백 개선(canShare 미지원 대응, 배치 실패 시 다운로드 대신 10장씩 나눠 공유).
- **사진 정렬 반전** — 먼저 올린(오래된) 사진이 위, 나중 사진이 아래(그리드·라이트박스 동일, 같은 날짜는 올린 순서). 기존 사진도 자동 적용(표시 정렬만 변경).
- **업로드 속도** — 3장씩 동시 업로드(현장자료·영수증·출금·카톡공유). 원본 화질 유지·순서 보존. *대표가 화질 압축은 거부 — 압축 없이 병렬화만.*

### 알림 보강
- 빠져 있던 등록 지점에 전원 알림 추가: **새 현장 등록 · 공정 추가(대시보드/현장상세) · 비용 자료 등록(금액은 알림에 미노출) · 출금 건 사진 추가**.
- 재정관리·직원정보는 **의도적으로 알림 제외**(관리자 전용 민감정보). 회사서류 '관리자만' 문서도 미알림.

### 실행 필요 SQL (Supabase, 대표 실행 여부 확인 필요)
- `db/payroll_ledger.sql` — 급여대장 전체 보기용 테이블 (⛔ 실행 후 해당 월 엑셀 재업로드 필요)
- `db/profit_file.sql` — 현장별 이익 첨부 컬럼 2개 (⛔)

### 다음 후보
- 사진 업로드 화질 선택(보통/원본) 옵션 — 대표 원하면. → **완료(17장)**
- 구역별 보기 정렬도 올린 순서로 통일 여부.

## 17. 알림 사고 복구 + 협업 기능 확장 (2026-07-09 ~ 07-10)

### 🚨 알림 전면 미발송 사고 (발견·복구)
- **증상**: 7/2 11:27 이후 인앱 알림·푸시 전부 미발송 (태블릿 테스트로 발견).
- **원인**: Vercel `SUPABASE_SERVICE_ROLE_KEY` 값 맨 앞에 **BOM(U+FEFF) 문자**가 섞여(붙여넣기 사고) 서버의 모든 service-role DB 요청 실패. 조용히 실패해 장기간 미발견. 임시 진단 API(`/api/diag`, 토큰 보호)로 운영 DB 직접 확인해 원인 특정.
- **수정**: `lib/supabase-admin.ts` `createAdminClient()`/`cleanEnv()` — 환경변수 BOM·공백 자동 제거. 모든 서버 라우트(push/create-user/sns) 적용, VAPID 키에도 적용. 구독 8기기 테스트 푸시 전체 SENT 확인, 모바일 수신 확인.
- **알림 켜기/끄기 개선**: 권한만 보고 '받는 중' 표시하던 것을 **실제 구독 여부 기준**으로. 끄기 버튼 신설, 켜기/끄기 실패 시 이유 표시.

### 협력업체 현장별 접근 권한 (잔디식) — 로드맵 A 항목 해결
- `project_access`(project_id+user_id) + `has_project_access()` — **partner는 공개 지정된 현장만** (목록·직접 URL 모두 DB 차단). projects/schedules/project_assignments/project_files 쓰기는 직원만. `db/project_access.sql`
- **새 현장 등록 시 공개할 업체 체크** + 현장 상세 **🔒 외부공개**에서 중간 초대/내보내기. 직원 누구나 관리.

### 거래처 연락처 공유
- `/contacts` 📇 — 업체명·분야(칩 선택)·담당자·전화(탭=바로 전화, 복사 버튼)·메모. 검색+분야 필터, 통합검색 포함. 직원 공용(RLS admin/designer/field), partner 차단. `db/contacts.sql` + 초기 18곳 `db/contacts_seed.sql`.

### 자료 이동/공유 (앱 안에서)
- 현장 자료 다중 선택 → **📁 이동**: ①현장 ②분류 모두 선택 후 [이동] 버튼(실수 방지 2단계). 공사전→시공사진 재분류, 다른 현장 이송.
- **💬 채팅 공유**: 선택 자료를 재업로드 없이 채팅 메시지로 (나와의채팅/전체방/방/1:1, 사진들은 묶음 1건).

### 채팅 고도화
- **사진 묶음 전송(카톡식)** — 여러 장=한 말풍선 격자(+N), 큰보기 좌우넘김 전체 포함. `messages.images` jsonb (`db/chat_images.sql`). 알림도 "사진 N장" 1건.
- **완전 삭제** — 흔적("삭제된 메시지") 없이 제거, 첨부·반응 정리, 상대 화면 실시간 반영.
- **여러 줄 입력** — PC: Enter 전송·Shift+Enter 줄바꿈 / 모바일: 줄바꿈 키+전송 버튼.
- **드래그 개선** — 채팅 화면 아무 데나 놓아도 전송(빗나가면 브라우저가 파일 열던 문제 해결).
- **파일 클릭=바로 보기** — 다운로드 대신 뷰어(PDF/오피스), 저장은 별도 버튼.

### 사진 업로드 속도/안정
- **3장 동시 업로드**(현장·영수증·출금·공유) — 원본 유지, 순서 보존.
- **빠른 업로드(기본)** — 긴변 2000px JPEG 축소(카톡 방식·압축파일 아님), 원본 옵션 토글. *대표가 초기 거부 → 원인(ZIP 오해) 해소 후 기본 채택.*
- **HEIC 안정화** — 사파리는 기기 내장 변환(즉시), heic2any는 한 장씩+30초 타임아웃 시 원본 업로드(멈춤 방지).

### 보기/표시 개선
- **모바일 속도** — 현장 사진 격자를 Vercel 이미지 최적화 썸네일(수십 KB)로, 영수증·출금·채팅 지연 로딩.
- **다운로드 없이 보기 확대** — 현장 목록·회사서류 엑셀/워드=오피스 뷰어. 사진 분류에 섞인 PDF=📄 문서 카드(클릭=뷰어), 큰보기 넘김에서 제외.
- **영수증·출금 문서 업로드 허용** — 사진 제한 해제, 문서는 📄 카드 표시.
- **미팅내용 글만 저장** — 파일 없이 텍스트 등록(📝, 여러 줄, 목록에 전문 표시).
- **사진 정렬** — 최신 날짜가 맨 위(대표 요청으로 두 차례 조정 끝 확정).

### 이름 변경
- 시공전사진 → **공사전사진** (기존 자료 일괄 변경 SQL 제공, 과도기 호환). 직원 관리 → **회원 관리**, 재정관리 → **경영관리**.

### 보안 정비 (외부 리뷰 반영, security_status.md 상세)
- 구버전 db/*.sql 전체허용(auth_all) 잔재 제거 — 재실행해도 최신 RLS 유지. `rls_helpers.sql` 공통 함수 분리 + 새 DB 세팅 순서 문서화(CLAUDE.md).
- link-preview SSRF 방어(리다이렉트 재검사·IPv6·사설 DNS 차단·2MB 스트리밍). pending 채팅 insert 차단, 방 생성 직후 RLS 충돌 수정, 멘션 대상 서버 검증.
- `npm run lint -- --quiet` 통과 유지.

### 실행 필요 SQL (Supabase — 적용 여부 대표 확인 필요)
- `chat_images.sql`(채팅 사진 묶음) · `contacts.sql`+`contacts_seed.sql`(거래처) · `project_access.sql`(협력업체 현장권한) · `payroll_ledger.sql`(급여대장 전체보기) · `profit_file.sql`(손익표 첨부)
- 카테고리 일괄 변경: `update public.project_files set category='공사전사진' where category='시공전사진';`
- 운영 잔여 느슨 정책 2건: worklogs·push_subscriptions (16장 이후 안내됨)

## 18. 마무리 다듬기 (2026-07-10)

- **수금 관리 = 관리자 전용 확정** — 대표 지시("관리자만 봐야지"). 메뉴(ADMIN_ONLY_NAV)·페이지 가드·통합검색·수금 알림(roles=['admin'])·RLS 전부 admin만. push API broadcast에 **roles 필터**(서버 검증) 신설. → 11장 매트릭스 반영.
- **알림 딥링크** — 알림 클릭 시 해당 내용이 바로 열림: 공지 `?open=id`(상세 자동 오픈), 출금 `?open=id`(건 상세), 현장 `?tab=자료/공정/비용`(비용은 권한자만), 채팅 `?dm=`/`?room=`(해당 대화방 오픈). 인앱 목록·토스트·OS 푸시 모두 동일.
- **공지사항 수정** — 상세보기 ✏ 수정 버튼, 기존 내용 불러와 저장. 수정 시 알림 재발송 없음(등록 때만).
- **공지사항 링크** — 내용 속 URL 클릭 가능 + 첫 링크 미리보기 카드(채팅과 동일). 기존 공지에도 자동 적용.
- **자료 이동 2단계** — 📁 이동 모달: ①현장 ②분류 모두 선택 후 [이동] 버튼(즉시 이동 실수 방지, 이동 위치 미리 표시).
- 사진 분류의 PDF 문서 카드(16→17장 이어짐), 알림 켜기/끄기 실구독 기준 등은 17장 참조.

## 19. 편집·작성 기능 완성 + 기본 UX 원칙 (2026-07-10 오후 ~ 07-17)

### 운영 원칙 (대표 지시 — 이후 모든 개발에 적용)
> **"다른 앱(카톡·네이버·드라이브)에서 당연히 되는 기본 기능은 요청하지 않아도 함께 넣는다."**
> 새 기능을 만들 때 등록만 만들지 말고 **수정·삭제·검색·알림·모바일 동작까지 기본 세트**로 포함할 것.

### 공지사항 — 블로그식 설명서 작성
- **이미지 첨부**: 캡처 Ctrl+V·파일 선택(여러 장), 자동 크기 축소, 썸네일 ×삭제, 목록 📷N 배지. `notices.images` jsonb — `db/notice_images.sql`
- **글·사진 번갈아 작성(블로그식)**: 글 쓰다 붙여넣으면 커서 위치에 `[사진N]` 토큰 삽입 → 볼 때 그 자리에 이미지 인라인 표시(클릭=크게). 토큰 없는 이미지는 하단 그리드(호환). 캐드 설명서 같은 매뉴얼 작성 용도.

### 현장 자료 — 편집 전면 지원
- **모든 분류 수정**: 목록 '수정' 버튼(전 항목) + 사진 큰보기 ✏ — 제목·분류(선택형)·메모(사진=구역/공간), 구매링크는 URL·메모까지.
- **파일 교체**: 수정 창에서 수정본 도면 등을 클릭·드래그·Ctrl+V로 교체(FileDropInput) — 항목 유지, 옛 파일 자동 정리, 제목 자동 갱신.

### 알림 UX
- **클릭 = 해당 내용으로 이동 + 목록에서 자동 제거**(확인한 알림은 사라짐). 안 누른 알림은 유지, '모두 지우기' 별도.

### 기타 편의 (기본 UX 점검 결과 보강)
- **채팅 날짜 구분선** — 오늘/어제/7월 10일 (금) (카톡식).
- **채팅 메시지 복사** — ⋯ 메뉴 📋 복사.
- **영수증 메모 수정** — 카드 '수정' 버튼(전엔 재업로드 필요).
- **영수증·출금 🖨 바로 인쇄** — 이미지는 인쇄창 즉시, PDF는 브라우저 뷰어. (PC '내보내기'는 윈도우 공유 한계 → 인쇄/저장 안내)
- 근태 관리 별도 메뉴는 만들었다가 **대표 결정으로 철회** — 직원정보내역의 직원별 근태 기능으로 일원화.

### 보안 후속 (외부 리뷰 반영)
- rls_money↔project_access의 project_files 정책 충돌 정리 — **어느 파일을 나중에 실행해도 partner=공개 현장만** 유지(동일 정책+선행요소 내장). 실행 순서 문서에 project_access.sql(8단계, 필수) 추가. payments 주석 admin 전용으로 정정. 상세: security_status.md.

### 실행 필요 SQL 추가분
- `db/notice_images.sql` — 공지 이미지 첨부용 (notices.images jsonb)
- ~~기존 미확인분~~ → **전부 적용 완료(2026-07-17)** — 대표가 일괄 SQL 실행(Success). 상세: security_status.md 헤더.

## 20. 버그 수정 2건 (2026-07-17)

### 🐛 모바일 채팅 입력창 실종 (전 기기, 7/2부터 잠복)
- **증상**: 폰(갤럭시 S25 확인)에서 채팅 입력창이 안 보임 — 답장 시도하다 발견. 스크린샷 제보로 확인: 입력창이 하단 초록 탭바 **뒤에 깔려** 있었음.
- **원인**: 채팅 래퍼에 `flex-1`(basis 0)과 `h-[calc(100dvh-3.5rem)]`이 동시 적용 → flex-1이 지정 높이를 무시하고 100vh로 늘어남 → 하단 3.5rem(입력창)이 고정 탭바에 가림. 7/2 "입력창 in-flow 전환" 때부터 모든 모바일에서 발생(PC는 가로 배치라 무관).
- **수정 과정 기록**: ①자동 스크롤+포커스 ②dvh 폴백 — 두 번 오진 후, ③flex-basis 분석으로 확정. 모바일 `w-full`(그로우 제거)·데스크탑만 `md:flex-1`. **갤럭시에서 정상 확인됨.**
- 부수 개선: 답장/수정 시 입력창 자동 스크롤+커서, 안드로이드 키보드 시 화면 축소(viewport interactive-widget), 아이폰 하단 안전영역 여백.

### 🐛 알림 딥링크가 "앱 켜진 상태"에서 무시됨
- **증상**: 구매링크·시공사진 알림 클릭 시 현장 **현황 탭**으로만 감(자료 탭 미이동).
- **원인**: 페이지가 이미 열려 있으면 재활용되는데, ?tab=/?dm=/?open= 처리 코드가 "최초 1회"만 실행되던 구조.
- **수정**: 현장(?tab)·채팅(?dm/?room)·공지/출금(?open) 전부 useSearchParams 반응형으로 — 주소 변화에 실시간 반응.

### 파일 교체 알림 정책 (대표 문의 → 확정)
- **파일을 실제로 교체**(도면 v2 등)하면 알림 발송("OO현장 · 자료 교체 · 파일명") — 옛 버전으로 작업하는 사고 방지.
- **제목·메모·분류만 수정**은 알림 없음(소음 방지).

## 21. 사진 저장 최적화 + 채팅 읽음 확인 (2026-07-29)

### 📄 PDF 보기 안정화
- **증상**: PDF 열면 구글 문서 뷰어가 "미리보기가 없음" 실패(큰 파일·구글 혼잡 시 빈발).
- **수정**: PDF는 구글 뷰어를 거치지 않고 **브라우저 내장 PDF 뷰어로 직접 열기**로 전면 전환(6곳: media 공용함수·회사서류·현장자료·비용첨부·경영관리 2곳). 엑셀·워드는 브라우저가 못 열어 MS 오피스 뷰어 유지.

### 🔍 검색 버튼이 목록 마지막 줄을 가림
- 우하단 🔍 플로팅 버튼이 표 마지막 줄의 수정/삭제 버튼을 덮음 → 목록 있는 **전 화면(14곳)** 데스크탑 하단 여백 확대(`md:pb-24`)로 마지막 줄이 버튼 위로 스크롤되게 수정.

### 📸 현장사진 Storage 비용·속도 최적화 (10항목)
1. 업로드 시 **긴 변 2400px 이하 자동 리사이즈** (기존 "빠른/원본" 선택 버튼 제거 — 항상 자동)
2. **WebP 품질 80%** 압축 (미지원 브라우저는 JPEG 폴백) — 동영상·문서·GIF는 원본 그대로
3. 원본과 별도로 **500px 썸네일** 생성(`files/{현장}/thumbs/`) — 실패해도 업로드는 계속
4. 사진 목록(격자)은 **썸네일만 로드**, 5. **클릭 시에만 고화질** 원본 로드(라이트박스)
6. 화면에 보이는 사진만 lazy loading (next/image + loading=lazy 유지)
7. 사진 격자 **30장씩 페이지네이션** (분류·구역별 ◀ n/m ▶, 날짜순/구역별 모두)
8. **동일 이미지 중복 업로드 방지** — 원본 SHA-256 지문 비교(파일명 달라도 감지), 건너뛴 목록 안내
9. **용량 표시**: 파일별(문서 목록·사진 호버) + 분류별 합계 + 현장별 누적("이 현장 자료 용량")
10. **관리자 Storage 경고**: 회원 관리 상단에 전체 사용량 게이지(무료 플랜 1GB 기준, 80% 이상 경고·95% 빨강)
- **DB**: `db/photo_optimize.sql` — project_files에 thumb_url/file_size/file_hash 컬럼+인덱스, `storage_total_bytes()`(security definer, admin만 값 반환). **SQL 미실행 상태여도 업로드는 되게** 컬럼 없음 오류 시 기존 방식 재시도 폴백 내장.
- 삭제·교체·덮어쓰기 시 **썸네일도 함께 삭제**(용량 누수 방지). 기존 사진은 그대로(신규 업로드부터 적용).
- 파이프라인: `lib/image.ts` — compressImage(2400/0.8 WebP)·makeThumbnail(500)·hashFile(SHA-256)·formatBytes.

### 💬 채팅 "누가 읽었는지" 확인 (카톡식)
- 단체방: **모든 메시지**에 안 읽은 사람 수(노란 숫자) 표시 — 기존엔 내 메시지만이었음. 모두 읽으면 내 메시지는 '읽음'.
- **숫자 클릭 → 팝업**: ✓ 읽은 사람 / ○ 안 읽은 사람 **이름 목록** (보낸 사람은 읽음에 포함, 나·삭제 메시지 제외).
- chat_reads에 user_id별 시각이 이미 있어 **추가 SQL 불필요**, 실시간 구독으로 읽는 순간 숫자 감소.
- 1:1은 기존대로 내 메시지에 '읽음' 표시.

## 22. 운영 다듬기 (2026-07-30)

### 📄 PDF 탭 제목을 파일명으로
- **증상**: 도면 PDF를 열면 탭 제목이 저장 주소의 숫자 이름(1785…pdf)으로 표시.
- **수정**: 제목을 붙인 래퍼 페이지(embed)로 열어 **탭 제목 = 앱에 보이는 파일명**(예: 0730_세무사사무실(최종)_YJ.pdf). PDF 여는 6곳 전체 적용, 기존 파일에도 즉시 적용. 모바일은 기기별 내장 뷰어 차이로 기존 방식(직접 열기) 유지.

### 👥 회원 내보내기 (관리자)
- 회원 관리에 **내보내기** 버튼(본인 제외) — `/api/admin/delete-user`(admin 검증+service role).
- 계정 삭제(로그인 불가) + 개인설정 정리(push_subscriptions·chat_room_members·chat_reads·notifications·project_access·profiles).
- **올린 자료·메시지·작업일지는 보존**(현장 기록 유지). 되돌릴 수 없음을 확인창에 명시.

### 💾 Storage 한도 기준 정정
- 게이지가 "3.72GB / 1GB (100%)" 빨간 경고로 표시 → 대표 확인 결과 **Pro 요금제** → 기준을 100GB로 정정, "3.72GB / 100GB (4%)" 정상 표시. (플랜 변경 시 `admin/users`의 LIMIT_GB 숫자만 수정)

## 23. 업로드 입력 통일 — 파일·이미지·캡처·드래그 어디서나 (2026-07-30)

> 원칙: **모든 업로드 지점**에서 파일·이미지·텍스트가 다 들어가고, 다른 곳에서 **드래그해서 놓기**와 **캡처/복사한 파일 Ctrl+V**가 동일하게 동작해야 한다.

### 📢 공지사항 파일 첨부 (PDF 등) — 신규
- 기존엔 이미지(캡처)만 첨부 가능 → **PDF·엑셀 등 일반 파일 첨부** 추가.
- 첨부 방법 3가지 모두 지원: 클릭 선택 · 등록창에 **드래그&드롭** · 캡처나 복사한 파일 **Ctrl+V**.
- 자동 분배: 이미지는 기존처럼 글 속 `[사진N]` 위치에, PDF 등 파일은 아래 첨부 목록으로(원본 파일명 유지).
- 상세 화면: 📎 첨부 클릭=바로 열기(PDF는 22장 래퍼로 탭 제목=파일명), 옆 "저장"=다운로드. 목록 카드에 📎 개수 표시.
- **DB**: `db/notice_files.sql` — notices에 `files jsonb` 컬럼. 미실행 상태면 저장 시 실행 안내 알림. (2026-07-30 운영 DB 적용 완료)

### 📋 전 업로드칸 "복사한 파일 Ctrl+V" 통일
- 기존: 붙여넣기가 **캡처(이미지)만** 인식 → 탐색기에서 복사한 **일반 파일(PDF 등)도 Ctrl+V로 업로드**되게 전 화면 통일.
- 적용 지점: 채팅 입력창(이미지 여러 장=한 묶음, 파일은 개별 전송), 현장 자료 업로드(DropZone), 영수증·출금요청(MultiFileZone), 공용 업로드칸 FileDropInput(회사서류·경영관리 손익표/매출매입/견적 등).
- 드래그&드롭·클릭 선택·텍스트 입력은 각 화면에 이미 있던 기능 그대로 유지 — 이로써 업로드 UX가 전 화면 동일해짐.

## 23. 사진 파일명 날짜 규칙 — NAS 날짜순 정렬 (2026-07-30)

### 문제
- 직원이 폰 "공유하기"로 올린 시공·마감사진을 NAS로 받으면 날짜순 정렬이 깨짐.
- 원인 2가지: ① 폰·앱이 공유 시 파일명을 image.jpg 등으로 바꿔버림 ② KakaoTalk_20260615_…/Resized_20260701_… 처럼 **날짜가 이름 속에 있어도 맨 앞이 아니면** 폴더 정렬(맨 앞 글자 기준)이 뒤죽박죽.

### 해결 (3중 규칙 — 핵심: 사진·동영상 이름은 반드시 날짜로 시작)
1. **업로드 시**: 날짜로 시작하면 그대로 / 날짜가 속에 있으면 앞으로 끌어옴(20260615_KakaoTalk_…) / 날짜가 없으면 촬영시각(lastModified)으로 YYYYMMDD_HHMMSS 이름 부여. `lib/image.ts dateStampedName()` — 현장 자료 추가·폰 공유 수신 두 경로 모두.
2. **다운로드 시**(전체 저장·저장·내보내기): 날짜로 시작하지 않는 옛 파일은 fileDate(이름 속 날짜→없으면 업로드일)를 앞에 붙여 저장 — `nasName()`.
3. **예전 자료 일괄 정리 SQL 2회 실행 완료**: `db/rename_old_photos.sql`(날짜 없는 사진에 업로드일 접두어) + `db/rename_old_photos2.sql`(속 날짜를 맨 앞으로). 사진·동영상만, 문서·링크 제외, 재실행 안전.
- 대표 확인: 북면 반찬가게 전체 저장 → 날짜순 정렬 정상.

### 폰 공유 수신 경로도 사진 최적화 적용
- 공유 수신 화면(share)이 최적화를 안 타고 원본 그대로 올리던 것 발견 → 현장 상세와 동일하게 **2400px WebP 압축 + 500px 썸네일 + 용량·SHA-256 지문 기록** 적용. 컬럼 없음 오류 시 기존 방식 폴백 동일.

### 한계(기록)
- 예전에 이름이 완전히 바뀐 사진(image.jpg)은 촬영 시각(시·분)까지는 복구 불가 — 같은 날 안에서의 순서는 근사치. 신규 업로드부터는 촬영시각까지 보존.

## 24. 채팅방 공지 (2026-07-31)

### 📢 대화방 상단 고정 공지 (카톡식)
- 대화방 헤더 📢 버튼 → 공지 등록/수정, 배너의 [수정], 작성창의 [공지 내리기]로 삭제.
- 전체 채팅·단체방·1:1 모두 방마다 하나씩. 1:1은 두 사람 id를 정렬한 공용 키라 양쪽이 같은 공지를 봄.
- 상단 초록 배너 고정: 접힌 상태(한 줄) ↔ 눌러서 펼치기(전문+작성자·날짜). 아주 길면 배너 안 스크롤(최대 45vh) — 채팅 화면을 다 덮지 않게.
- 실시간 반영(supabase_realtime), 기존 📌 핀 메시지 배너와 별개로 공존.
- **용도 확장**: 디자인팀 방 공지를 팀 게시판처럼 사용(진행프로젝트 목록 등) → 작성창을 넓은 모달+화면 60% 높이 입력칸으로 확대.
- **버그 기록**: 작성칸에 h-[55vh]를 줬는데 flex-1이 높이를 무시해 몇 줄로 쪼그라듦(§20 모바일 입력창과 동일 계열 — flex-1의 basis 0이 지정 높이를 무시) → flex-1 제거로 해결. 교훈: flex 자식에 고정 높이를 줄 땐 flex-1과 같이 쓰지 말 것.
- **DB**: `db/chat_notices.sql` — chat_notices(conv_key pk, content, author_name, updated_at), RLS는 승인 직원(협력업체 제외), realtime 등록. **실행 완료.**

## 25. 시스템 내 전달 체계 완성 (2026-07-31)

### ➦ 채팅 메시지 전달 (카톡식)
- 메시지 ⋯ 메뉴 → [➦ 전달] → 대상 선택(전체 채팅방/단체방/나와의 채팅(보관)/직원 1:1).
- 글·사진(여러 장 묶음 통째)·파일 모두. 저장소 파일은 재업로드 없이 같은 URL로 새 메시지 — 즉시 전달·용량 증가 없음. 받는 방에 알림(딥링크).

### 💬 현장 자료 → 채팅 전달 (전 카테고리)
- **파일별 바로 전달**: 문서 목록(도면·견적서·계약서 등) 각 행에 [💬 전달], 사진 타일 메뉴에 [💬] — 한 번에 대상 선택창.
- **여러 개**: ☑︎ 선택 → 액션바 [💬 채팅] (기존). 사진은 묶음 1건, 문서는 각각 파일 메시지.
- 링크·글 메모 항목은 제외(파일이 아님). 구현: chatShareOne(단일 선택→기존 sendToChat 재사용).

### 🐛 태블릿에서 액션 버튼 실종
- **증상**: 갤럭시 탭에서 채팅 ⋯ 메뉴가 안 보임 (폰은 정상).
- **원인**: 화면폭(md) 기준으로 "넓으면 PC 취급 → 마우스 올려야 표시"인데 태블릿은 넓은 화면+터치라 마우스가 없어 영영 안 보임.
- **수정**: 숨김 기준을 화면폭(md:) → **마우스 있는 기기(@media(hover:hover))**로 전환. 채팅 ⋯, 사진 선택(✓)·크게보기(⛶), 출금 삭제 버튼 4곳. 교훈: hover에 숨긴 버튼은 반드시 hover:hover 미디어로 — 태블릿 = 넓은 화면 + 터치.

## 26. 작업일지 임시저장 + 급여대장 누락 수정 + 목록 정렬 (2026-08-01 ~ 08-11)

### 📒 작업일지 임시저장 → 퇴근 시 제출
- 업무가 있을 때마다 적고 [임시저장](작성중 상태, **본인만 보임**·알림 없음) → 헤더 버튼이 [✏ 일지 이어쓰기](노랑)로 바뀌어 몇 번이든 추가 작성 → 퇴근 시 [제출하기] 또는 목록 카드의 [제출]로 공개+알림 **1회만**.
- 제출된 일지 수정은 기존대로 조용히. DB: `db/worklog_draft.sql`(status 컬럼, 기존 행은 '제출'로 채움) — **실행 완료**. 컬럼 없음 오류 시 기존 방식 폴백 내장.

### 🐛 7월 급여대장 4명 중 3명만 업로드
- **원인**: 엑셀에서 신규 입사자(방유진 7/20) 행에 급여합계 수식(SUM)이 복사되지 않아 칸이 비어 있었고, 파서가 "급여합계 없는 행=빈 행"으로 건너뜀.
- **수정**: 급여합계 칸이 비면 **기본급~상여금을 직접 합산**해 인식(excel-parse 2곳). NAS 원본 파일로 4명 전원 파싱 검증(방유진 847,741원 = 기본급+식대). 재업로드 시 월 범위 삭제 후 재삽입이라 중복 없음.
- 대표에게 엑셀 원본 수식(N9)도 채워두길 권고함.

### ↕ 목록 정렬 선택 (공용 SortSelect)
- 경영관리 견적서(최신/오래된/현장명/금액↑↓ — 대표 요청), 매출매입(월/금액/구분), 현장 관리(최신 등록/현장명/착공일/상태), 거래처(업체명/최신 등록/분야).
- 이미 정렬 있는 곳은 유지: 현장 사진(날짜/구역), 작업일지·영수증·출금(날짜), 회사 서류(분류 묶음).

## 27. 전면 감사 1~4단계 완료 (2026-08-12 ~ 08-13)

대표 지시로 시스템 전면 검토·보강을 4단계로 진행. 총 57개 파일 수정, 커밋 d8628f2~e387e41.

### 1단계 — 권한과 보안
- 🔴 **권한 상승 취약점 발견·차단**: profiles가 전체 허용(auth_all)이라 로그인만 하면 자기 role을 admin으로 바꿀 수 있었음 → `db/rls_step1_hardening.sql`(읽기/관리자만 수정·삭제, role 5종 체크 제약, 공지 역할별, 알림 임의생성 차단) **실행 완료**.
- 역할 단일화: 미사용 'staff' 제거, admin/designer/field/partner+pending 확정. create-user API 서버 역할 검증.
- canEdit(null)=true 버그 수정 → 내부 직원 3역할만 true.
- **가입 초대제 전환**: 공개 /signup 폐쇄. [직원 추가]→초대 링크(7일·1회용, `invite_tokens`) 카톡 전달→직원이 /invite에서 비밀번호 직접 설정+자동 로그인. Supabase 기본 메일 발송 제한 때문에 메일 대신 링크 방식 채택.
- 보안 헤더 6종(CSP·XFO·nosniff·Referrer·Permissions·HSTS) — Supabase/실시간/PDF래퍼/링크미리보기/푸시 예외 명시.
- RLS 역할×테이블 매트릭스 검증(§11 표 참고): 기존 정책 대부분 양호, profiles·notices·notifications만 교체.

### 2단계 — 자동 테스트 (Playwright E2E 20개, `npm run test:e2e`)
- 빌드→로컬(3100)→전용 계정 5개(e2e-*@jmtest.local) 자동 생성→테스트→**계정·E2E_ 데이터 전부 자동 삭제**(잔재 0건 검증).
- 커버: 미로그인(페이지·API 401), pending 전면 차단(+DB 직접조회 0건+권한상승 시도 차단), 역할별 메뉴, 현장 CRUD·자료, 출금 흐름, 채팅 송수신·읽음·알림(브라우저 2개), 회원 생성·삭제·초대링크, partner RLS(금전 0건·지정 현장만·쓰기 불가), 모바일.
- 발견 수정: 미로그인 API가 401 대신 로그인 HTML 반환 → /api/*는 401 JSON.
- 로컬 실행에 SUPABASE_SERVICE_ROLE_KEY를 .env.local에 수동 입력 필요(vercel pull은 Sensitive 미포함).

### 3단계 — 코드 품질·안정성
- **lint 경고 48→0** (규칙 끄기 없이): effect 동기 setState 15건(파생 상태·렌더 중 보정 패턴·마이크로태스크 — ref는 렌더 중 접근 금지라 가드도 state로), img 17건 next/image(unoptimized) 전환, deps 7건(useCallback화), 미사용 7건, 기존 eslint-disable 2건도 제거 후 실수정.
- 🐛 **채팅 최신 메시지 잘림 수정**: '오래된 순 300개' 로드라 300개 초과 대화에서 최신이 안 보였음 → 최신 100개+[↑ 이전 대화 보기] 페이지네이션(스크롤 유지).
- 대화 전환 깜빡임 원천 차단(렌더 중 정리), 네트워크 오류 시 빈 화면 대신 안내+[다시 시도].
- **Toaster 도입**: alert 63곳→토스트(실패=빨강/성공=초록 자동). confirm(파괴적 확인)과 긴 안내 2곳은 의도적으로 유지.

### 4단계 — 운영 준비
- middleware→**proxy**(src/proxy.ts) 이전 — Next16 deprecation 해소, 동작 동일(E2E 검증).
- **감사 로그**: `db/audit_logs.sql` — DB 트리거로 12개 테이블(회원권한/직원·급여·근태/급여내역·대장/비용/출금/수금/서류·자료 삭제/접근권한) 변경 전후 자동 기록. 수정·삭제 불가(정책 없음), admin 조회만. 화면: 관리자>📋 감사 기록. **실행 완료**.
- **마지막 관리자 보호**: DB 트리거+delete-user API 이중. 본인 삭제 방지 기존 유지.
- **비밀번호 재설정**: 회원 관리 [🔑 비번 재설정]→3일 링크(카톡 전달)→/reset에서 직원이 새 비밀번호. `db/reset_tokens.sql`(kind/target_user) **실행 완료**.
- env 누락 시 원인 표시 오류, 파일 500MB 제한을 채팅·공유·서류로 확대.
- 검토만(구현 보류): 로그인 속도제한(Supabase 내장), 백업(Pro 일일 7일 — 대시보드 복원), 오류추적(Vercel Logs로 충분), 삭제복구(완전삭제 정책+백업+감사기록).

### 남은 위험 (로드맵)
- **Storage public 버킷** — URL 아는 사람은 로그인 없이 파일 열람 가능. 서명 URL 전환은 별도 큰 작업.
- E2E가 운영 DB 공유(전용 계정+접두어+정리로 격리) — 완전 분리는 별도 Supabase 프로젝트 필요.
- CSP는 실사용 검증 완료 항목 외 신규 외부 리소스 추가 시 헤더 갱신 필요(next.config.ts).

## 28. 현장 선택 목록 정리 (2026-08-14)

- **문제**: 시공·마감사진 올릴 때(폰 공유) 현장 선택 목록에 끝난 현장까지 전부 나와 불편. 대표는 차후 업로드 가능성 때문에 끝난 현장을 지우고 싶지 않음.
- **해결**: 상태(완료·중단) 기준으로 목록을 두 그룹으로 분리 — 지우지 않고 정리.
  - 폰 공유 화면 '저장할 현장': 🏗️ 진행 중(위, 기본 선택) / ✅ 완료·중단(아래 묶음) optgroup.
  - 자료 이동 모달: 진행 중만 표시 + [▼ 완료·중단 현장 N개 보기] 접기/펼치기.
- 대표가 끝난 현장들 상태를 '완료'로 일괄 변경 완료 → 목록 즉시 짧아짐. 완료 현장·자료는 그대로 보존(현장 관리의 '완료·중단 현장 보기' 체크로 언제든 접근).
- 작업 중 기록: bash로 백틱 포함 JSX를 패치하다 템플릿 리터럴이 깨져 빌드 실패 → Edit 도구로 복구. 교훈(재확인): 한글·백틱 섞인 코드 패치는 bash 문자열 대신 Edit 도구.

## 29. 운영 가이드 — 직원이 비밀번호를 잊었을 때 (2026-08-21)

이 시스템은 메일 발송 없이 **관리자가 재설정 링크를 발급해 카톡으로 전달**하는 방식이다(§27 4단계에서 구축).

**절차 (관리자):**
1. [회원 관리] 메뉴 → 해당 직원 줄의 **[🔑 비번 재설정]** 클릭
2. 재설정 링크가 생성되고 **자동 복사**됨 (화면에도 표시)
3. 링크를 **카톡으로 직원에게 전달**
4. 직원이 링크를 열어 **새 비밀번호를 직접 설정** → 즉시 자동 로그인

**규칙:**
- 링크는 **3일 유효·1회용**. 만료되면 같은 방법으로 재발급
- 아이디(이메일)는 그대로, 비밀번호만 변경됨
- 관리자는 새 비밀번호를 알 수 없음(직원 본인만)
- 모든 계정 관련 변경은 감사 기록에 남음

## 30. Storage 보안 1단계 — 경영관리 파일 잠금 보관함 (2026-08-25)

- **배경**: uploads 버킷이 공개라 파일 주소를 아는 사람은 로그인 없이 열람 가능(§27 남은 위험 1순위). 민감한 것부터 단계적 전환 시작.
- **1단계 범위**: 경영관리 파일 — 견적서·매출매입 첨부·손익표.
- **구조**: 비공개 버킷 `secure`(storage RLS: **관리자만** 읽기·쓰기·삭제) + DB에는 `secure://finance/...` 형태로 저장 + 열 때마다 **1시간짜리 서명 URL** 발급(`lib/media.ts resolveFileUrl`). 링크가 유출돼도 1시간 뒤 만료, 발급 자체가 권한 검사를 거침. 삭제는 공용 `removeStoredFile`(공개/잠금 양쪽 인식).
- **이사 완료**: `scripts/migrate-finance-secure.mjs`(서비스 키, 로컬 실행)로 기존 견적서 23건 이동 — 실패 0.
- **검증(실측)**: DB 23/23 잠금 전환 ✓ · 옛 공개 주소 400 차단 ✓ · 잠금 버킷 공개경로 400 차단 ✓ · 서명 주소 200 정상 ✓.
- **DB**: `db/secure_bucket.sql` **실행 완료**.
- **다음 단계(로드맵)**: 2단계 회사 서류(company_documents — 직원 열람이라 역할별 정책 필요), 3단계 현장 자료(수천 파일·썸네일·채팅 전달 연계라 가장 큼).

## 31. 사용성 다듬기 모음 (2026-08-20 ~ 08-26)

### 🕐 업로드 시각 표시
- 현장 자료(문서 행)·공지·영수증·출금: 날짜만 → **날짜+시간**. 작업일지 카드에 '올린 시각' 추가(업무 날짜와 별도).

### 🖨 현장 자료 크게 보기에 버튼 추가
- 라이트박스 우하단: **인쇄**(영수증과 동일 방식, 동영상 제외)·내보내기·저장·수정. 손글씨 미팅 메모를 바로 출력하는 용도.

### 📱 안읽음 표시 전 기기 동기화
- **문제**: 채팅 대화별 안읽음이 기기(localStorage)에만 저장돼 폰에서 읽어도 PC 배지가 남음.
- **수정**: 서버(chat_reads)의 내 읽은 시각을 기준으로 계산(전체 채팅 포함 — markMyRead가 'all' 키도 기록). 다른 기기에서 읽으면 실시간으로 배지 제거. 알림(🔔)·사이드바 배지는 원래 서버 기반이라 이미 동기화 상태였음. OS 푸시 팝업은 기기 OS 소관이라 대상 외.

### 📄 문서 다운로드 파일명 수정
- **문제**: NAS 사진 정렬용 날짜 접두어(§23)가 PDF·문서에도 붙어 저장한 제목과 달라짐.
- **수정**: 날짜 접두어는 **사진·동영상만**, 문서는 저장한 제목 그대로 (nasName에 미디어 판별 추가).
